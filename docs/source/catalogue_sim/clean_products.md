clean\_products script
===================================

This is a helper Python script to delete intermediary simulation
products - it does not need to be called anywhere by a user and
can be left as a background helper file. 